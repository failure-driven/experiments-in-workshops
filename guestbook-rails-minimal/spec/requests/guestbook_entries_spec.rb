require "rails_helper"

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/guestbook_entries" do
  # This should return the minimal set of attributes required to create a valid
  # GuestbookEntry. As you add validations to GuestbookEntry, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      GuestbookEntry.create! valid_attributes
      get guestbook_entries_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      guestbook_entry = GuestbookEntry.create! valid_attributes
      get guestbook_entry_url(guestbook_entry)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_guestbook_entry_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      guestbook_entry = GuestbookEntry.create! valid_attributes
      get edit_guestbook_entry_url(guestbook_entry)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new GuestbookEntry" do
        expect {
          post guestbook_entries_url, params: {guestbook_entry: valid_attributes}
        }.to change(GuestbookEntry, :count).by(1)
      end

      it "redirects to the created guestbook_entry" do
        post guestbook_entries_url, params: {guestbook_entry: valid_attributes}
        expect(response).to redirect_to(guestbook_entry_url(GuestbookEntry.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new GuestbookEntry" do
        expect {
          post guestbook_entries_url, params: {guestbook_entry: invalid_attributes}
        }.not_to change(GuestbookEntry, :count)
      end

      it "renders a response with 422 status (i.e. to display the 'new' template)" do
        post guestbook_entries_url, params: {guestbook_entry: invalid_attributes}
        expect(response).to have_http_status(:unprocessable_entity)
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested guestbook_entry" do
        guestbook_entry = GuestbookEntry.create! valid_attributes
        patch guestbook_entry_url(guestbook_entry), params: {guestbook_entry: new_attributes}
        guestbook_entry.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the guestbook_entry" do
        guestbook_entry = GuestbookEntry.create! valid_attributes
        patch guestbook_entry_url(guestbook_entry), params: {guestbook_entry: new_attributes}
        guestbook_entry.reload
        expect(response).to redirect_to(guestbook_entry_url(guestbook_entry))
      end
    end

    context "with invalid parameters" do
      it "renders a response with 422 status (i.e. to display the 'edit' template)" do
        guestbook_entry = GuestbookEntry.create! valid_attributes
        patch guestbook_entry_url(guestbook_entry), params: {guestbook_entry: invalid_attributes}
        expect(response).to have_http_status(:unprocessable_entity)
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested guestbook_entry" do
      guestbook_entry = GuestbookEntry.create! valid_attributes
      expect {
        delete guestbook_entry_url(guestbook_entry)
      }.to change(GuestbookEntry, :count).by(-1)
    end

    it "redirects to the guestbook_entries list" do
      guestbook_entry = GuestbookEntry.create! valid_attributes
      delete guestbook_entry_url(guestbook_entry)
      expect(response).to redirect_to(guestbook_entries_url)
    end
  end
end
