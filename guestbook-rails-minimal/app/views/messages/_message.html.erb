<div id="<%= dom_id message %>" class="card-content" data-testid="message-<%= dom_id message %>">
  <p>
    <strong>Text:</strong>
    <% if message.generated_text.present? %>
      <span data-testid="text-generated"><%= message.generated_text %></span>
    <% else %>
      <span data-testid="text-message"><%= message.text %></span>
    <% end %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= message.name %>
  </p>

</div>
